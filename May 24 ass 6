def reverseString(self, s: List[str]) -› None:
    def myrev(left, right, 1):
         if left>=right:
             return
         s[left],s[right]=s[right],s[left]
         myrev(left+1, right-1, s)
    myrev(left=@, right=len(s)-1,1=s)
